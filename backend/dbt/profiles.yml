globe:
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('DB_HOST', 'localhost') }}"
      user: "{{ env_var('DB_USER', 'postgres') }}"
      password: "{{ env_var('PASSWORD', 'yeezus9090') }}"
      port: "{{ env_var('PORT', '5432') | int }}"
      dbname: "{{ env_var('NAME', 'globe') }}"
      schema: public
      threads: 4
  target: dev
